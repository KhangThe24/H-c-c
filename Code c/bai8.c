#include<stdio.h>
// toán tử phép toán 
/*
    toán tử toán học: +, -, *, /, %, ++, --
    toan tử so sánh: ==, !=, > ,>=, <, <=
    toán tử gán: =, +=, 8=, -=, /=, %=
    toán tử logic: && // !
*/
int main(){
    int a = 10, b = 20 ;
    int c, d;
    c = a+b;
    printf("c=%d",c);
    return 0;
}